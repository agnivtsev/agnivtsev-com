baseURL: https://example.com
languageCode: ru-ru
defaultContentLanguage: ru
title: Александр Агнивцев
theme: PaperMod

params:
  cover:
    linkFullImages: true

  outputs:
    home:
      - HTML
      - RSS
      - JSON

  disableThemeToggle: true

menu:
  main:
    - identifier: tags
      name: Теги
      url: /tags/
      weight: 10
    - identifier: search
      name: Поиск
      url: /search/
      weight: 20
    - identifier: archives
      name: Архив
      url: /archives/
      weight: 30
